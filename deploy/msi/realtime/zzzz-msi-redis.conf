# MSI development backend only. Existing jeeb-redis publishes DB0 on loopback6380.
# Preserve the native development process's pool5; do not alter auth or MIX_ENV.
[Service]
Environment="REDIS_URL=redis://127.0.0.1:6380/0"
Environment="REDIS_POOL_SIZE=5"
