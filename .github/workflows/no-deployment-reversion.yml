name: No deployment reversion

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Reject deployment reversion behavior
        run: python3 scripts/check-forward-only-policy.py
